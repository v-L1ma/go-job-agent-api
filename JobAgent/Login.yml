info:
  name: Login
  type: http

http:
  method: POST
  url: http://localhost:1323/api/v1/login
  body:
    type: json
    data: |-
      {
        "email":"teste@teste.com",
        "password":"Vinicius@1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
